apiVersion: apps/v1
kind: Deployment
metadata:
  name: guestbook-argocd
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: guestbook-argocd
      team: sre
  template:
    metadata:
      labels:
        app: guestbook-argocd
    spec:
      containers:
        - image: gcr.io/google-samples/gb-frontend:v5
          name: guestbook-argocd
          ports:
            - containerPort: 80
